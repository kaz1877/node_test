{
  "name": "cloud-run-app",
  "version": "1.0.0",
  "description": "A simple Node.js application for Google Cloud Run",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.16.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.2"
  }
}
